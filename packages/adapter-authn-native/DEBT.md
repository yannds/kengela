# DEBT.md - @kengela/adapter-authn-native

> The port is an airlock, not a hideout. Debts tracked, removed once resolved.

| #   | Subject                                      | State  | Note                                                                                                                                                                                                                                                 | Target                                                                                                   | Prio |
| --- | -------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---- |
| 2   | AES-GCM master key rotation                  | assume | No `enc:vN` versioning nor rotation/rollover.                                                                                                                                                                                                        | Version header + re-encryption at rollover                                                               | P3   |
| 3   | TOTP code anti-replay (NIST 800-63B 5.1.4.2) | scope  | The challenge (`MfaChallengeStore`) is one-shot, but the TOTP CODE itself is not memorized: within the step window (~30 s) an already consumed valid code could be replayed via a NEW `challengeId`. The verifier should refuse an already used OTP. | Anti-replay cache (tenant:user:code -> seen) with TTL = TOTP window, wired into `TotpMfaService.verify`. | P2   |
